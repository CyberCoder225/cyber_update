{
  "name": "my-apps-website",
  "version": "1.0.0",
  "description": "My Apps Collection Website",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "vite": "^4.4.0",
    "terser": "^5.19.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
